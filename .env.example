# Prefilled (still editable) coordinator URL on the login screen.
# Copy to .env.local for a local override -- see vite.config.ts, standard
# Vite env-file loading, nothing custom here.
VITE_DEFAULT_SERVER_URL=https://coordination-server.twing.dev
