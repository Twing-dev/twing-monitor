# Copy to .env (gitignored) next to this file to override which
# coordinator the built dashboard talks to by default. Not required for
# the standard deployment -- see this directory's README.md.
VITE_DEFAULT_SERVER_URL=https://coordination-server.twing.dev
